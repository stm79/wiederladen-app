DATABASE_URL="file:../data/db/wiederladen.db"
UPLOADS_DIR="./data/uploads"

# Only needed once served through a reverse proxy on a real domain — the
# public domain it serves this app under, e.g.:
# DOMAIN=wiederladen.example.com
